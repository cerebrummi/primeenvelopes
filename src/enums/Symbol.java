package enums;

/** Symbols 
 * 
 * The symbol L means undetermined whether prime or composite number. The
 * symbol M means composite number and the symbol P means prime number. The
 * symbol 1 means the number 1 which is not a prime number.
 */

public enum Symbol
{
   ONE, L, M, P;
}
